{{template "base" .}}

{{define "content"}}
    <div class="row">
        <div class="col">
            <h1 class="text-center mt-4">This will be the contact page</h1>
        </div>
    </div>
{{end}}